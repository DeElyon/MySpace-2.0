{
  "name": "job-service",
  "version": "1.0.0",
  "description": "Job Management Service for My Space FPA (Python/FastAPI)",
  "private": true,
  "scripts": {
    "dev": "echo 'Job service is a Python service. Use: python -m pip install -r requirements.txt && uvicorn app.main:app --reload'",
    "start": "echo 'Job service is a Python service. Use: python -m pip install -r requirements.txt && uvicorn app.main:app'",
    "build": "echo 'Job service is a Python service. See requirements.txt for dependencies'"
  }
}
